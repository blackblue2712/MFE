name: UNIT_TEST
on:
  push:
    branches:
      - main
jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Install Dependencies 🔰
        run: yarn i
      - name: Run Build 🛠
        run: yarn run build
      - name: Run unit test
        run: yarn run test