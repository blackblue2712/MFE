name: UNIT_TEST
on:
  push:
    branches:
      - main
jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Install Dependencies 🔰
        run: yarn
      - name: Run Build 🛠
        run: yarn build
      - name: Run unit test
        run: yarn test